- proxy = MiniRocket::FilterProxy.new(resource_class, parent, self)

#filter.col-lg-12 class="#{proxy.active? ? 'collapsed' : 'collapse'}"
  = proxy.render(rocket_builder.filter.html_options, &rocket_builder.filter.block)
